# Export — SPL Gestion de Suivi

Archive prête : `spl-gestion-de-suivi-deploy.zip`.

Elle contient l’application complète, la PWA installable et tous les guides. Elle exclut `.env`, les secrets, `node_modules` et `.next`.

Pour Vercel, une seule variable est nécessaire :

```text
DATABASE_URL=URL_POSTGRESQL_NEON
```
